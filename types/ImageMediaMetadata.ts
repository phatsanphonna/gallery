export interface ImageMediaMetadata {
    width: number,
    height: number,
    rotation: number,
    time: string,
    cameraMake: string,
    cameraModel: string,
    exposureTime: number,
    aperture: number,
    flashUsed: boolean,
    focalLength: number,
    isoSpeed: number,
    meteringMode: string,
    exposureMode: string,
    colorSpace: string,
    whiteBalance: string,
    exposureBias: number,
    maxApertureValue: number
}